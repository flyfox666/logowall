# Host port the wall is exposed on (change if 8080 is taken), e.g. PORT=8081
PORT=8080

# Admin token - CHANGE THIS before deploying to production!
ADMIN_TOKEN=change-this-to-a-strong-password

# Optional: LOG_LEVEL=info | warning | error | debug
#LOG_LEVEL=info
